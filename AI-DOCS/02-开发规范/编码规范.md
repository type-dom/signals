# TypeDOM Signals - 编码规范

> 📋 **Signals 响应式系统编码标准**  
> ✅ Class + Reactive Pattern 最佳实践

---

## 📖 概述

本文档定义了 `@type-dom/signals` 库的编码规范，确保代码质量、一致性和可维护性。

### 适用范围

- ✅ Signal/Computed/Effect 实现代码
- ✅ 响应式系统核心逻辑
- ✅ 测试用例编写
- ✅ 性能优化代码

---

## 🎯 核心原则

### 1. Push-Pull 模型优先

```typescript
// ✅ 推荐：遵循 Push-Pull 模式
function propagate(link: Link): void {
  // Push 阶段：标记依赖为 dirty/pending
  sub.flags = flags | ReactiveFlags.Pending;
  
  // Pull 阶段：惰性求值时检查并更新
  if (checkDirty(deps, node)) {
    update(node);
  }
}

// ❌ 不推荐：递归调用导致栈溢出
function propagateRecursive(link: Link): void {
  propagate(link.nextSub); // 递归深度过大
}
```

### 2. 性能约束遵守

```typescript
// ✅ 推荐：避免使用 Array/Set/Map
interface ReactiveNode {
  deps?: Link;      // 使用 Link 链表
  depsTail?: Link;
  subs?: Link;
  subsTail?: Link;
  flags: ReactiveFlags;
}

// ❌ 不推荐：使用复杂数据结构
interface BadNode {
  deps: Map<string, any>;  // 性能开销大
  subs: Set<any>;
}
```

### 3. 类属性限制 (< 10 个)

```typescript
// ✅ 推荐：精简的类定义 (7 个属性)
export class Signal<T = unknown> implements SignalNode<T> {
  pendingValue: T | undefined;      // 1
  currentValue: T | undefined;      // 2
  subs?: Link;                      // 3
  subsTail?: Link;                  // 4
  flags: ReactiveFlags;             // 5
  
  constructor(initialValue?: T) {   // 方法不计入
    // ...
  }
  
  get() { /* ... */ }               // 方法不计入
  set(value: T): void { /* ... */ } // 方法不计入
}

// ❌ 不推荐：过多的类属性
class TooManyProperties {
  prop1: any; prop2: any; prop3: any;
  prop4: any; prop5: any; prop6: any;
  prop7: any; prop8: any; prop9: any;
  prop10: any; prop11: any; // 超出 V8 快速属性优化范围
}
```

---

## 📝 TypeScript 类型定义规范

### 接口命名规范

```typescript
// ✅ 推荐：使用 Node 后缀表示节点类型
interface ReactiveNode {
  deps?: Link;
  flags: ReactiveFlags;
}

interface SignalNode<T = any> extends ReactiveNode {
  currentValue?: T;
  pendingValue?: T;
}

interface ComputedNode<T = any, S = T> extends ReactiveNode {
  value: T | undefined;
  getter: (previousValue?: S) => T;
  setter?: (newValue: S) => void;
}

interface EffectNode extends ReactiveNode {
  fn(): void;
}

// ✅ 推荐：使用 Link 表示连接关系
interface Link {
  version: number;
  dep: ReactiveNode;
  sub: ReactiveNode;
  prevSub: Link | undefined;
  nextSub: Link | undefined;
  prevDep: Link | undefined;
  nextDep: Link | undefined;
}
```

### 枚举定义规范

```typescript
// ✅ 推荐：使用 const enum 优化性能
export const enum ReactiveFlags {
  None = 0,           // 000000
  Mutable = 1,        // 000001
  Watching = 2,       // 000010
  RecursedCheck = 4,  // 000100
  Recursed = 8,       // 001000
  Dirty = 16,         // 010000
  Pending = 32,       // 100000
}

// 使用位运算组合标志
const flags = ReactiveFlags.Mutable | ReactiveFlags.Dirty; // 17
```

### 泛型使用规范

```typescript
// ✅ 推荐：明确的泛型约束
export function signal<T>(): Signal<T>;
export function signal<T>(initialValue: T): Signal<T>;
export function signal<T>(initialValue?: T): Signal<T> {
  return new Signal<T>(initialValue);
}

// ✅ 推荐：计算属性的泛型支持
export function computed<T>(
  getter: (previousValue?: T) => T,
  setter?: (newValue: T) => void
): Computed<T> {
  return new Computed(getter, setter);
}

// ❌ 不推荐：缺少泛型导致类型推断失败
export function computed(getter: () => any) {
  return new Computed(getter); // 丢失类型信息
}
```

---

## 🔧 实现规范

### Signal 实现规范

```typescript
// ✅ 推荐：完整的 Signal 实现
export class Signal<T = unknown> implements SignalNode<T> {
  pendingValue: T | undefined;
  currentValue: T | undefined;
  subs?: Link;
  subsTail?: Link;
  flags: ReactiveFlags;

  constructor(initialValue?: T) {
    if (arguments.length) {
      this.pendingValue = initialValue;
      this.currentValue = initialValue;
    }
    this.flags = ReactiveFlags.Mutable;
  }

  get() {
    // 1. 检查是否需要更新
    if (this.flags & ReactiveFlags.Dirty) {
      if (updateSignal(this)) {
        const subs = this.subs;
        if (subs !== undefined) {
          shallowPropagate(subs);
        }
      }
    }
    
    // 2. 依赖追踪
    if (activeSub !== undefined) {
      linkReactiveNode(this, activeSub);
    }
    
    // 3. 返回值
    return this.pendingValue!;
  }

  set(value: T) {
    // 1. 相同值不触发更新
    if (this.pendingValue === value) {
      return;
    }
    
    // 2. 设置新值
    this.pendingValue = value;
    this.flags |= ReactiveFlags.Dirty;
    
    // 3. 触发更新
    if (this.subs !== undefined) {
      shallowPropagate(this.subs);
    }
    
    // 4. 如果不是批量更新，立即 flush
    if (!batchDepth) {
      flush();
    }
  }
}
```

### Computed 实现规范

```typescript
// ✅ 推荐：Computed 实现
export class Computed<T = any, S = T> implements ComputedNode<T, S> {
  private _value: T | undefined;
  private _flags: ReactiveFlags = ReactiveFlags.Dirty;
  private _deps?: Link;
  private _depsTail?: Link;
  private _subs?: Link;
  private _subsTail?: Link;
  private readonly _getter: (previousValue?: T) => T;
  private readonly _setter?: (newValue: T) => void;

  constructor(
    getter: (previousValue?: T) => T,
    setter?: (newValue: T) => void
  ) {
    this._getter = getter;
    this._setter = setter;
  }

  get() {
    // 1. 依赖追踪
    if (activeSub !== undefined) {
      linkReactiveNode(this, activeSub);
    }
    
    // 2. 惰性求值
    if (this._flags & ReactiveFlags.Dirty) {
      const oldValue = this._value;
      this._value = this._getter(oldValue);
      this._flags &= ~ReactiveFlags.Dirty;
      
      // 3. 如果有 setter 且值变化，触发更新
      if (this._setter && this._value !== oldValue) {
        this._setter(this._value);
      }
    }
    
    return this._value!;
  }
}
```

---

## ⚡ 性能优化规范

### 1. 位运算优化

```typescript
// ✅ 推荐：使用位运算检查和设置标志
if (node.flags & ReactiveFlags.Dirty) {
  // 需要更新
}

node.flags |= ReactiveFlags.Watching;  // 设置标志
node.flags &= ~ReactiveFlags.Pending;  // 清除标志

// ❌ 不推荐：使用布尔字段
interface Node {
  isDirty: boolean;    // 占用更多内存
  isWatching: boolean;
  isPending: boolean;
}
```

### 2. 对象池模式

```typescript
// ✅ 推荐：复用 Link 对象
const linkPool: Link[] = [];

function createLink(): Link {
  return linkPool.pop() || {
    version: 0,
    dep: null!,
    sub: null!,
    prevSub: undefined,
    nextSub: undefined,
    prevDep: undefined,
    nextDep: undefined,
  };
}

function releaseLink(link: Link) {
  linkPool.push(link);
}
```

### 3. 惰性求值

```typescript
// ✅ 推荐：只在访问时计算
const computed = computed(() => {
  console.log('Computing...'); // 只在第一次 get() 或依赖变化时执行
  return expensiveOperation();
});

// 不会触发计算
// const val = computed; 

// 会触发计算
const val = computed.get();
```

---

## 🧪 测试编写规范

### 测试命名规范

```typescript
// ✅ 推荐：清晰的描述性命名
test('should create signal with initial value');
test('should trigger effect when value changes');
test('should not trigger if value is same');

// ❌ 不推荐：模糊的命名
test('basic test');
test('test 1');
```

### 测试结构规范

```typescript
describe('Signal', () => {
  describe('Initialization', () => {
    test('should create with undefined value');
    test('should create with initial value');
  });
  
  describe('Get operations', () => {
    test('should return current value');
    test('should track dependencies');
  });
  
  describe('Set operations', () => {
    test('should update value');
    test('should trigger effects');
  });
});
```

### 测试覆盖率要求

```typescript
// ✅ 推荐：覆盖所有边界情况
describe('signal.set()', () => {
  test('should update value');
  test('should not trigger for same value');
  test('should handle null values');
  test('should handle undefined values');
  test('should handle object references');
});

// 覆盖率目标：
// - 行覆盖率 ≥ 90%
// - 分支覆盖率 ≥ 90%
// - 函数覆盖率 ≥ 90%
```

---

## 📊 代码审查清单

### 自查清单

在提交代码前，请确认：

- [ ] 遵循 Push-Pull 模式
- [ ] 无递归调用（使用栈结构）
- [ ] 类属性 < 10 个
- [ ] 使用 const enum
- [ ] 位运算检查标志
- [ ] 泛型定义完整
- [ ] 无 Array/Set/Map 滥用
- [ ] 测试覆盖率达标
- [ ] 注释清晰准确

### 互查清单

审查他人代码时，重点关注：

- [ ] 架构设计合理性
- [ ] 性能优化有效性
- [ ] 类型安全完整性
- [ ] 边界情况处理
- [ ] 错误处理机制
- [ ] 代码可读性
- [ ] 测试用例充分性

---

## 🔗 相关文档

- [00-索引.md](./00-索引.md) - 文档导航
- [01-项目概述/项目背景.md](./01-项目概述/项目背景.md) - 项目介绍
- [02-开发规范/测试规范.md](./02-开发规范/测试规范.md) - 测试要求
- [03-API 文档/接口定义.md](./03-API 文档/接口定义.md) - API 参考

---

**维护者**: TypeDOM Core Team  
**最后更新**: 2026-03-18  
**许可**: MIT License
